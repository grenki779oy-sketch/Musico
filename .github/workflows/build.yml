name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🐳 Use Kivy Docker
      run: |
        docker pull kivy/buildozer
        docker run -it --rm \
          -v $PWD:/home/user/hostcwd \
          kivy/buildozer \
          buildozer android debug
          
    - name: 📦 Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Spotify-APK
        path: bin/*.apk
