name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04  # Используем старую версию где есть sdkmanager
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🐍 Setup
      run: |
        sudo apt-get update
        sudo apt-get install -y openjdk-11-jdk python3-pip
        pip install buildozer cython
        
    - name: 🏗️ Build
      run: |
        buildozer android debug
        
    - name: 📦 Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Spotify-APK
        path: bin/*.apk
